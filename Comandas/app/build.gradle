apply plugin: 'com.android.application'

android {
    compileSdkVersion 22
    buildToolsVersion '25.0.0'

    defaultConfig {
        applicationId "es.vallesoft.comandas"
        minSdkVersion 15
        targetSdkVersion 20
        versionCode 1
        versionName "1.0"

    }
    buildTypes {
        release {
            apply plugin: 'eclipse'
         }
    }
}

dependencies {
    compile 'com.android.support:support-v4:22.0.0'
}
